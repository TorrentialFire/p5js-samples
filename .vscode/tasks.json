{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start browser-sync",
            "type": "shell",
            "command": "browser-sync start --server '${fileDirname}' --files '${fileDirname}' --port 8080"
        }
    ]
}